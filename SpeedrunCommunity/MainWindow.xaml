<Window x:Class="SpeedrunCommunity.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeedrunCommunity"
        mc:Ignorable="d"
        Title="Speedrun Community" Height="350" Width="600" WindowStartupLocation="CenterScreen">

    <StackPanel>
        <TabControl Background="Transparent">
            <!-- PLAYERS TAB -->
            <TabItem Header="Players" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Players}" SelectedItem="{Binding SelectedPlayer}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                            <DataGridTextColumn Header="Nick" Binding="{Binding Nick}" Width="*"/>
                            <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding PlayerSearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="32" Margin="10,0,0,0" Background="{x:Null}" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="80" Height="32" Margin="10,0,0,0" Style="{StaticResource AccentButtonStyle}" Command="{Binding SearchPlayerCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="5" Width="80" Height="32" Style="{StaticResource AccentButtonStyle}" Command="{Binding AddPlayerCommand}"/>
                            <Button Content="Edit" Margin="5" Width="80" Height="32" Command="{Binding EditPlayerCommand}"/>
                            <Button Content="Delete" Margin="5,5,10,5" Width="80" Height="32" Command="{Binding DeletePlayerCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
            
            <!-- GAMES TAB -->
            <TabItem Header="Games" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Games}" SelectedItem="{Binding SelectedGame}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                         <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Developer" Binding="{Binding Developer}" Width="*"/>
                            <DataGridTextColumn Header="Year" Binding="{Binding ReleaseYear}" Width="60"/>
                            <DataGridTextColumn Header="Platform" Binding="{Binding Platform.Name}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding GameSearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="32" Margin="10,0,0,0" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="80" Height="32" Margin="10,0,0,0" Style="{StaticResource AccentButtonStyle}" Command="{Binding SearchGameCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="5" Width="80" Height="32" Style="{StaticResource AccentButtonStyle}" Command="{Binding AddGameCommand}"/>
                            <Button Content="Edit" Margin="5" Width="80" Height="32" Command="{Binding EditGameCommand}"/>
                            <Button Content="Delete" Margin="5,5,10,5" Width="80" Height="32" Command="{Binding DeleteGameCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
            
            <!-- RESULTS TAB -->
            <TabItem Header="Results" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Player" Binding="{Binding Player.Nick}" Width="*"/>
                            <DataGridTextColumn Header="Game" Binding="{Binding Game.Name}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="*"/>
                            <DataGridTextColumn Header="Time (s)" Binding="{Binding Time}" Width="60"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding ResultSearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="32" Margin="10,0,0,0" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="80" Height="32" Margin="10,0,0,0" Style="{StaticResource AccentButtonStyle}" Command="{Binding SearchResultCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="5" Width="80" Height="32" Style="{StaticResource AccentButtonStyle}" Command="{Binding AddResultCommand}"/>
                            <Button Content="Edit" Margin="5" Width="80" Height="32" Command="{Binding EditResultCommand}"/>
                            <Button Content="Delete" Margin="5,5,10,5" Width="80" Height="32" Command="{Binding DeleteResultCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </StackPanel>
</Window>
