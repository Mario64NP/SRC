<Window x:Class="SpeedrunCommunity.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeedrunCommunity"
        mc:Ignorable="d"
        Title="Speedrun Community" Height="350" Width="600" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Height" Value="25"/>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Width" To="100" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Width" To="75" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.Background>
        <LinearGradientBrush EndPoint="0,0" StartPoint="1,1">
            <GradientStop Color="LightBlue"/>
            <GradientStop Color="MediumPurple" Offset="0.5"/>
            <GradientStop Color="Purple" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <StackPanel>
        <TabControl Background="Transparent">
            <!-- PLAYERS TAB -->
            <TabItem Header="Players" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Players}" SelectedItem="{Binding SelectedPlayer}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                            <DataGridTextColumn Header="Nick" Binding="{Binding Nick}" Width="*"/>
                            <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding PlayerSearchText, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="25" Margin="10,0,0,0" Background="{x:Null}" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="70" Style="{StaticResource SearchButtonStyle}" Command="{Binding SearchPlayerCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="10" Width="70" Height="25" Command="{Binding AddPlayerCommand}"/>
                            <Button Content="Edit" Margin="10" Width="70" Height="25" Command="{Binding EditPlayerCommand}"/>
                            <Button Content="Delete" Margin="10" Width="70" Height="25" Command="{Binding DeletePlayerCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
            
            <!-- GAMES TAB -->
            <TabItem Header="Games" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Games}" SelectedItem="{Binding SelectedGame}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                         <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Developer" Binding="{Binding Developer}" Width="*"/>
                            <DataGridTextColumn Header="Year" Binding="{Binding ReleaseYear}" Width="60"/>
                            <DataGridTextColumn Header="Platform" Binding="{Binding Platform.Name}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding GameSearchText, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="25" Margin="10,0,0,0" Background="{x:Null}" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="60" Style="{StaticResource SearchButtonStyle}" Command="{Binding SearchGameCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="10" Width="60" Height="25" Command="{Binding AddGameCommand}"/>
                            <Button Content="Edit" Margin="10" Width="60" Height="25" Command="{Binding EditGameCommand}"/>
                            <Button Content="Delete" Margin="10" Width="60" Height="25" Command="{Binding DeleteGameCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
            
            <!-- RESULTS TAB -->
            <TabItem Header="Results" Background="Transparent">
                <StackPanel Background="Transparent">
                    <DataGrid ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" Height="200" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Player" Binding="{Binding Player.Nick}" Width="*"/>
                            <DataGridTextColumn Header="Game" Binding="{Binding Game.Name}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="*"/>
                            <DataGridTextColumn Header="Time (s)" Binding="{Binding Time}" Width="60"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding ResultSearchText, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="25" Margin="10,0,0,0" Background="{x:Null}" VerticalContentAlignment="Center"/>
                            <Button Content="Search" Width="60" Style="{StaticResource SearchButtonStyle}" Command="{Binding SearchResultCommand}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Add" Margin="10" Width="60" Height="25" Command="{Binding AddResultCommand}"/>
                            <Button Content="Edit" Margin="10" Width="60" Height="25" Command="{Binding EditResultCommand}"/>
                            <Button Content="Delete" Margin="10" Width="60" Height="25" Command="{Binding DeleteResultCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </StackPanel>
</Window>
